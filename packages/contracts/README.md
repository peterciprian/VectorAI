# Shared Contracts

Cross-service request, response, and event schemas belong here. The first implementation will add generated TypeScript types and Python/Pydantic models as API contracts stabilize.
